layer: tool
language: javascript

tasks:
  test:
    command: [pnpm, ava, test]
    inputs:
      - "src/**/*"
      - "test/**/*"
      - "package.json"
      - "ava.config.js"
    deps:
      - sdk-wasm:build

  lint:
    command: [echo, "No linting configured"]

  ci:
    deps: [test]
